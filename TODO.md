# TODO for EduStudentReports Project Setup (Updated)

- [x] Create initial structure with FE (Next.js .js) and BE (Node.js .js)
- [x] Update FE to TypeScript (.tsx files): Change package.json, rename files to .tsx, add types
- [x] Update BE to TypeScript (.ts files): Change package.json, rename api files to .ts, add types
- [x] Add BE API for teacher login: POST /api/login with phone number, simulate verification (for demo, return success)
- [x] Add BE API for teacher to enter/update student grades: POST /api/grades, require auth (simple token)
- [x] Integrate MongoDB for data storage: Add .env, mongoose, models, update APIs to use MongoDB instead of JSON
- [ ] Update FE components to .tsx with types
- [ ] Add FE teacher login page/component (optional, since focus on BE first)
- [ ] Verify updated structure and sample code for Vercel deployment
